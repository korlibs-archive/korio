apply plugin: 'kotlin-platform-jvm'

dependencies {
	expectedBy project(":korio-common")
	compile "com.soywiz:klock:$klockVersion"
	compile "com.soywiz:korinject:$korinjectVersion"
	if (kmemVersion != '') compile "com.soywiz:kmem:$kmemVersion"
	compile "com.soywiz:kds:$kdsVersion"
}
