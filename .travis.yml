language: java

jdk:
  - oraclejdk7

script:
  - ./gradlew -s -i check install distJs

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
